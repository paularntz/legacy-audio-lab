[build]
  publish = ".next"
  command = "npm run build"

# Netlify Forms configuration
[context.production.environment]
  NODE_ENV = "production"

# Redirect rules
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200